query RuleActionAssignProvider($vendorId: ID!) {
  vendor(memberId: $vendorId) {
    id
    performables {
      id
      name
    }
    providers {
      id
      member {
        id
        company
      }
    }
  }
}
