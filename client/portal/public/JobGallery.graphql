query JobGallery($jobId: ID!) {
  orderJob(jobId: $jobId) {
    buyerCanDownload
    order {
      id
      buyer {
        id
      }
    }
    buyerDeliverables {
      ...DeliverablePreview
    }
  }
}
